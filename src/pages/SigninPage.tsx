import { AuthFormNavigation, AuthLayout, SigninForm } from 'components/auth'

const SigninPage = () => (
  <AuthLayout>
    <AuthFormNavigation />
    <SigninForm />
  </AuthLayout>
)

export default SigninPage
